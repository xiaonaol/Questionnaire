server:
  port: 8085

mybatis:
  typeAliasesPackage: com.example.dao.entity
  mapperLocations: classpath:mapper/*.xml
  configuration:
    cache-enabled: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/questionnaire?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver